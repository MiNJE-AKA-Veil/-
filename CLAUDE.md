# SFC 기동 이해하기 — 설비 이해도 가이드북 (진행 기록)

이 문서는 ITNECO 지역난방/발전 설비의 SFC(Static Frequency Converter) 기동 방식을
"설비 이해도 향상 가이드북"의 한 섹션으로 만드는 작업의 진행 상황을 기록한다.
노션에 정리된 원본 자료를 바탕으로, (1) 서브 에이전트로 이론적 정확성을 검증하고
(2) 이해하기 쉬운 설명문으로 교정하고 (3) 인터랙티브 애니메이션으로 시각화하는
3단계 작업으로 진행 중이다. 다음 세션(또는 다른 작업자)이 이어서 작업할 수 있도록
지금까지의 결정, 검증된 사실, 만든 산출물, 남은 작업을 정리한다.

## 진행 중 지켜야 할 두 가지 원칙 (사용자가 명시적으로 설정)

이 두 원칙은 앞으로의 모든 단계에 계속 적용되어야 한다.

1. **"다음 단계로 넘어가기 전 Plan에 대해 설명하고 허락을 받을 것"** — 새로운 단계를
   구현하기 전에 반드시 계획을 설명하고 사용자의 승인을 받는다. 승인 없이 먼저
   만들지 않는다. (세션 초반, 요청 없이 먼저 페이지를 만들었다가 삭제 요청을 받은
   적이 있음 — 반드시 지킬 것.)
2. **"무조건 동의 하지 않도록 한다"** — 사용자가 제안한 내용이라도 기술적으로
   틀렸거나 의심스러우면 그대로 동의하지 말고 검증된 사실에 근거해 반박/보완한다.
   (아래 "바로잡은 오해" 참고.)

## 검증된 핵심 기술 사실 (서브 에이전트 검증 완료, 향후 재확인 없이 신뢰 가능)

- SFC(=LCI, Load Commutated Inverter)는 정지 상태의 동기발전기를 기동시키는 방식.
  6-펄스 싸이리스터 브릿지 = **정류기(AC→DC) + DC 링크 + 인버터(DC→가변주파수 AC)**.
- **정류기 점호각(α)** → DC 링크 전압/전류 크기(Vdc=Vdo·cos α), 토크 능력을 제어.
- **인버터 자체의 점호(진각 β, 소호각/여유각 γ≈15°)** → **출력 주파수**를 결정하고
  전류형성실패(commutation failure)를 방지. 이 둘은 서로 다른 제어축이다.
- **출력 주파수는 "점호 순서가 반복되는 속도"(T1→T2→…→T6 시퀀스 타이밍)가 결정한다.
  "래칭전류/래칭시간"이 결정하는 게 아니다** — 래칭전류/시간은 개별 SCR 하나가
  턴온에 성공하는지에 관한 문턱값일 뿐, 주파수와는 무관함. (이 오해가 대화 중
  두 번 재등장했음 — 아래 "바로잡은 오해" 참고.)
- 각 SCR은 전기각 **120° 통전 + 60° 휴지**를 반복 (토폴로지로 고정된 값). 항상
  상측 1개 + 하측 1개, 총 2개 소자만 동시 도통.
- 출력 전류 파형은 순수 사각파도 순수 펄스파도 아닌 **"계단형(stepped) 유사구형파"**.
- SFC는 발전기를 **정격의 약 80%까지** 가속한 뒤 계통/터빈에 분리 — 원본 노션 자료의
  수치이며 검증 완료.
- **입력(60Hz 고정 계통)과 출력(SFC 생성, 가변) 주파수는 서로 분리(decoupled)**된다 —
  정류해서 DC로 만드는 순간 입력의 주파수 정보는 사라지고, 인버터가 완전히 새로운
  주파수를 합성하기 때문. SFC의 핵심 기능이 바로 이 가변주파수 출력.
- 전기각(electrical angle) vs 기계각(mechanical angle) 구분. 상 표기는 R/S/T
  (한국 산업 관행, A/B/C 아님), 120° 간격.
- 실제 도통 테이블(반복 재사용):
  ```
  [{th:['T1','T6'],lv:{R:1,S:-1,T:0}}, {th:['T1','T2'],lv:{R:1,S:0,T:-1}},
   {th:['T3','T2'],lv:{R:0,S:1,T:-1}}, {th:['T3','T4'],lv:{R:-1,S:1,T:0}},
   {th:['T5','T4'],lv:{R:-1,S:0,T:1}}, {th:['T5','T6'],lv:{R:0,S:-1,T:1}}]
  ```

## 바로잡은 오해 (다시 나오지 않도록 주의)

- **"점호각이 곧 주파수를 조절한다"** → 아님. 정류기 점호각(α)은 DC 링크 전압/전류
  크기를 조절하는 것이고, 주파수는 인버터의 점호 시퀀스 타이밍이 조절한다.
- **"래칭시간으로 주파수를 조절한다"** → 아님. 래칭전류/시간은 SCR 개별 소자의
  턴온 문턱값(도통 성공 여부)이며 주파수 제어 변수가 아니다.
- **"SCR 도통 시간으로 주파수를 조절한다"** → 원인과 결과를 혼동한 것. 점호 시퀀스
  타이밍(독립변수)이 바뀌면 결과적으로 실제 도통 시간(초 단위)이 줄어드는 것이지,
  도통 시간 자체를 조작하는 게 아니다.
- **점호 타이밍 표시 단위: µs 아님, ms가 맞음.** 60° 스텝 하나당 간격 =
  1000/(6×f) ms. 정격 60Hz의 80%(48Hz)일 때 약 3.5ms, 저주파 기동 구간(예: 2Hz)일
  때 약 83ms — 전부 ms 단위 범위. µs 단위는 소호각(γ) 실시간이나 게이트 펄스 폭처럼
  다른 양에 해당하므로 여기 쓰면 혼동을 유발함.

## 애니메이션 산출물

- **파일 위치**: 저장소 내 `guidebook/sfc/sfc-sim.html` (커밋됨). 가이드북 프로젝트
  전체는 `guidebook/` 아래에 설비 섹션별 폴더로 쌓아갈 예정 (SFC가 첫 섹션, 앞으로
  다른 설비 섹션도 `guidebook/<섹션명>/` 형태로 추가). 작업 중에는 세션 스크래치패드
  에서 수정한 뒤 이 경로로 복사/커밋하는 방식으로 진행.
- **공개 링크(Artifact, private)**: https://claude.ai/artifact/XkzXk5NjzsdixK2dGDzYKb
  (제목 "SFC 기동 이해하기") — 같은 URL로 계속 갱신 중 (2026-09-19 기준 v23).
- **원래 4단계 계획** (사용자가 스토리보드 확정 후 지시한 순서):
  1. 전기각(electrical angle) — 3상 정현파 — **완료**
  2. SFC 싸이리스터 전류 흐름 (6-pulse 브릿지) — **완료**
  3. 정현파 → 구형파(계단형) 변환 — **완료(기본), 가변주파수 시연 기능 추가 중**
  4. 합성벡터(MMF)가 회전하는 것 — **미착수** (이전 폐기된 초안에는 있었으나,
     "처음부터 다시 시작" 이후의 현재 버전에는 아직 없음)

### 현재 구조 (섹션 1~3)

- **섹션 1 (입력, 고정 60Hz)**: 원형 벡터 다이어그램 + 정현파, R→S(120° 지연)→T(240°
  지연) 순서로 위상 등장(reveal) 애니메이션 1회 재생 후 정상상태 루프.
  `angleIn`, `IN_DEG_PER_SEC=30`(고정)으로 구동. `introDone` 플래그로 1회성 리빌 제어.
- **섹션 2 (6-Pulse 브릿지)**: T1~T6 싸이리스터 회로도, 현재 도통 중인 2개 소자와
  실제 전류 경로(DC+ → 상측 소자 → 노드 → 하측 소자 → DC−)를 화살표로 표시.
  `angleOut`으로 구동 (섹션 1과 **독립적인 클록**).
- **섹션 3 (계단형 출력 파형)**: R/S/T 3행 계단파. **x축은 전기각이 아니라 고정된
  실제 시간창(200ms)** — 주파수가 높을수록 같은 200ms 안에 더 많은(더 좁은) 주기가
  들어가도록 `buildOutChart(freqHz)`가 매번 다시 그림. 마커는 `angleOut`이 아니라
  독립된 실시간 스윕 클록(`sweepMs`, 200ms 주기 반복)으로 이동.

### 입력/출력 시계 분리 + 가변 주파수 기능 (최근 구현)

SFC의 핵심 특징(고정 60Hz 입력을 가변 주파수 출력으로 변환)을 보여주기 위해
클록을 분리함:

- **입력(섹션 1)**: `angleIn`, 항상 고정 속도로 진행. 리드아웃: "입력 전기각 XX°
  (60Hz 고정)".
- **출력(섹션 2·3)**: `angleOut`, 독립적인 가변 속도. 헤더의 점호 타이밍 프리셋
  버튼(`#freqRow`)으로 조절.
  - 프리셋: **12Hz(13.9ms) / 24Hz(6.9ms) / 36Hz(4.6ms) / 48Hz(3.5ms)** — 정격
    60Hz 기준 20/40/60/80% (검증된 "~80%에서 분리" 사실에 근거, 임의의 0~100% 범위
    아님). 기본값 24Hz(40%).
  - 버튼 라벨 형식은 `Hz(ms)` — ms는 점호 간격(60° 스텝 하나당 시간) =
    1000/(6×f).
  - 프리셋을 바꾸면 `outDegPerSec`가 갱신되고 섹션 3의 `buildOutChart()`가
    다시 그려짐. 섹션 1은 전혀 영향받지 않음.
- **"구간별 정지" 버튼(`#stepRow`)**: 이제 `angleOut`만 조작 (입력 위상각은
  그대로 흘러감).
- Playwright(헤드리스 크로미움, `/opt/pw-browsers/chromium`)로 12Hz vs 48Hz
  렌더링을 스크린샷 비교 검증함 — 200ms 창 안에 각각 약 2.4주기 / 9.6주기가
  들어가 파형 폭 차이가 시각적으로 명확히 드러나는 것 확인. 레이아웃 깨짐,
  JS 에러 없음.
- **슬로모션 재생 기능 추가 (2026-09-24 커밋)**: 실제 속도에서는 점호 간격이
  3.5~13.9ms로 60fps 화면 프레임(16.7ms)보다 빨라 2·3단계가 깜빡였음.
  `#slowRow`에 1/100·1/200·1/400배속 버튼 추가, `angleOut`과 `sweepMs`를
  동일한 `slowFactor`로 나눠 둘이 항상 같은 비율로 느려지게 함 (Hz(ms) 표시와
  200ms 축은 실제 값 유지).

## 버그 수정: 섹션 2·3 위상 불일치 (2026-09-24, 사용자 발견 후 검증·수정 완료)

사용자가 "3상 위상 단계별로 SCR 전류흐름과 구형파의 위치가 안 맞는 것 같다"고
지적 → 원칙 2에 따라 바로 동의하지 않고 코드를 계산으로 검증한 결과 **실제
버그로 확인됨**.

- **원인**: 3번 차트(계단형 파형)의 마커/그림은 고정된 `WINDOW_MS=200ms`를
  기준으로 `sweepMs % 200`으로 위치를 계산했는데, 200ms가 선택된 주파수의
  실제 출력 주기(`1000/Hz` ms)의 정수배가 아니었음 (12/24/36/48Hz 각각
  2.4/4.8/7.2/9.6주기 — 전부 소수). 반면 2번 회로도는 `angleOut`을 랩(wrap)
  없이 그대로 사용. 그 결과 **처음 200ms 동안은 두 섹션이 일치하지만, 마커가
  200ms마다 리셋될 때마다 2번(계속 흐르는 실제 위상)과 3번(200ms로 잘려
  리셋되는 위상)이 서로 다른 SCR 도통 구간(sextant)을 가리키게 됨** — 재생
  시간이 길어질수록 어긋난 상태로 계속 보임. 시뮬레이션으로 24Hz 기준
  S=200ms부터 계속 불일치가 재현됨을 확인 (5000ms 동안 미세 간격으로
  스캔해도 수정 전에는 다수 불일치, 수정 후에는 0건 — `node`로 직접 검증함).
- **수정**: `WINDOW_MS`를 고정 200ms 대신 `windowMsFor(hz)` 함수로
  "선택한 주파수의 실제 출력 주기(1000/hz ms)의 정수배 중 200ms에 가장 가까운
  값"으로 계산하도록 변경 (12Hz→2주기=166.7ms, 24Hz→5주기=208.3ms,
  36Hz→7주기=194.4ms, 48Hz→10주기=208.3ms). 이러면 마커가 리셋되는 순간이
  항상 위상 0°와 정확히 일치해서 2·3단계가 항상 일치함. 주파수 버튼 클릭 시
  `WINDOW_MS`를 재계산하고 `buildTimeAxis()`로 하단 시간축 눈금(0/25/50/75/100%
  지점, ms 라벨은 실제 windowMs 비례값)도 다시 그리도록 분리했음
  (`buildTimeAxis` 함수 신규 추가, 기존엔 고정 0/50/100/150/200 라벨이었음).
  "약 200ms" 창이라는 비교 취지는 유지됨 (166.7~208.3ms 사이로 소폭 변동).
- **검증**: `node`로 수정 전/후 로직을 그대로 재현해 5000ms 동안 1.3ms
  간격으로 스캔 → 4개 프리셋 전부 불일치 0건 확인. 인라인 `<script>` 문법도
  `node --check`로 확인함.
- Artifact v25에 반영, git 커밋 `9afc621`.

### 후속 수정 — `windowMsFor`만으로는 불충분했음 (2026-09-24, 사용자가 "아직도 안 맞는다"고 재확인 요청)

사용자가 v25 확인 후에도 여전히 안 맞아 보인다고 재차 지적 → 다시 계산 검증한 결과
**진짜 근본 원인은 따로 있었음**을 확인. `windowMsFor` 수정은 "주파수를 한 번도
안 바꾸고 같은 주파수로 계속 재생만 하는 경우"만 고쳤을 뿐, 아래 두 경우는
여전히 어긋났음 (둘 다 `node`로 재현·수치 확인):

1. **재생 중 주파수 프리셋을 바꾸는 경우**: `angleOut`과 (구)`sweepMs`는 별도로
   누적되는 두 개의 시계였고, 그 비례관계(`angleOut = 0.36×hz×sweepMs`)는
   "시작부터 같은 hz로 쭉 흘러왔을 때"만 성립함. 주파수를 바꾸면 그 시점까지
   누적된 값을 그대로 유지한 채 새 hz로 흐르기 시작하므로 비례관계가 깨짐
   (테스트: 24Hz 3초 재생 후 48Hz로 전환 → 이후 2초간 120프레임 중 22프레임
   불일치).
2. **"구간별 정지" 버튼(0°~360°) 클릭**: `angleOut`만 그 값으로 강제로 바꾸고
   (구)`sweepMs`는 손대지 않았기 때문에 클릭 시 거의 항상 불일치 (테스트:
   180° 버튼 클릭 시 2단계=구간3, 3단계=구간5로 확인).

**근본 수정**: "두 개의 시계를 같은 비율로 맞춰 굴리는" 방식 자체를 버리고,
`sweepMs` 변수를 완전히 제거. 3단계 마커 위치를 매 프레임 **오직 현재
`angleOut`값과 `activeFreqHz`로부터 직접 계산**하도록 바꿈 (별도 시계가 없으니
어긋날 방법 자체가 없어짐 — `render()`의 `sextantPos`/`totalSextants` 계산).
ms 단위로 변환하지 않고 "60°당 1구간" 단위(도 단위)로 직접 계산해서 60°
경계값(정확히 구간별 정지 버튼이 찍는 값들)에서 부동소수점 왕복 오차가 생기지
않도록 함.

**검증**: 실제 코드와 동일한 로직으로 "24Hz 3초 재생 → 48Hz 전환 → 2초 재생 →
구간별 정지 버튼 전부 클릭 → 12Hz 전환 → 5초 재생 → 버튼 전부 클릭 → 36Hz
전환 → 5초 재생" 시나리오를 `node`로 시뮬레이션 → 917개 체크 포인트 중
916개 일치, 나머지 1개는 검증 스크립트가 정확히 정수인 값을 부동소수점으로
왕복 계산하며 생긴 인위적 오차(실제 렌더링 코드는 비율값을 픽셀 좌표에만
바로 쓰므로 영향 없음)임을 재확인.
- Artifact v26에 반영, git 커밋 `204839e`.

### 세 번째 수정 — 로직은 맞았지만 CSS 트랜지션 때문에 시각적으로 여전히 어긋나 보였음 (2026-09-24)

사용자가 v26 확인 후 "파형이랑 SCR 점호 스위칭 타이밍이 아예 어긋난다"고 재차 지적,
"파형을 봤을 때 정확히 스위칭되는 소자가 동일하기를 원한다"고 명확히 요구.
데이터/로직 자체는 이미 정확했지만(917개 체크 중 916개 일치까지 검증됨),
**화면에 그리는 방식에 남아있던 문제**를 발견:

- 2단계 전류 화살표(`curTop`/`curMid`/`curBot`)의 `x1`/`x2`(위치)에
  **0.2초 CSS 트랜지션**이 걸려 있어서 도통 소자가 바뀔 때 위치가 슬라이드되며
  이동함.
- 2단계 싸이리스터 원(`circ`)의 `fill`/`stroke`(색상)에도 0.2초 트랜지션이
  걸려 있어서 활성화 색이 서서히 페이드인/아웃됨.
- 반면 3단계 파형 마커(`outMarker`)는 트랜지션이 전혀 없어 **매 프레임 즉시**
  정확한 위치로 점프.
- 실제 속도에서 한 스텝(60°) 지속시간은 12~48Hz 기준 3.5~13.9ms인데, 트랜지션은
  200ms나 걸림 → 2단계는 색/위치가 다음 스텝이 오기 전에 채 안착하지도 못하고
  계속 "따라가는 중"인 흐릿한 상태로 보이는 반면, 3단계는 이미 정확하게 딱딱
  끊어진 스텝을 보여줌 → 같은 순간을 봐도 두 섹션이 다른 상태처럼 보임
  (슬로모션을 켜도 스텝이 바뀐 직후 0.2초간은 여전히 "지연"되어 보임).
- **수정**: `curTop`/`curMid`/`curBot`의 인라인 트랜지션 제거, 싸이리스터
  원(`circ`)·라벨(`txt`)의 `style.transition` 설정 코드 제거. 이제 2단계도
  3단계와 마찬가지로 매 프레임 즉시 전환됨 (실제 SCR 전류천이도 마이크로초
  단위로 거의 즉각적이므로 물리적으로도 더 정확함). 전역 CSS
  `svg line, svg circle{transition:opacity .2s ease;}` 규칙은 그대로 둠 —
  1단계 인트로 리빌(위상 등장 페이드인)에만 쓰이고 2·3단계 동기화와는 무관.
- Artifact v27에 반영, git 커밋 `10e143e`.
- **사용자 재확인 결과: "문제가 전혀 개선되지 않았다"** (2026-09-24) — CSS 트랜지션 제거로도
  체감상 안 고쳐졌다는 피드백을 받음. **다음 세션에서 반드시 이어서 파야 할 미해결 이슈.**
  지금까지 세 번의 수정(windowMsFor, sweepMs 제거, CSS 트랜지션 제거)이 전부 "논리적으로는
  맞는 것을 수치로 검증"하는 방식이었는데도 사용자 체감은 여전히 안 맞는다는 것 — 이는
  (a) 아직 못 찾은 네 번째 원인이 있거나, (b) 애초에 사용자가 "안 맞다"고 느끼는 기준 자체가
  지금까지 다뤄온 "sextant 인덱스 일치"와는 다른 것(예: 색상 의미 혼동, 화살표 방향, 구간
  경계의 시각적 애매함 등)일 가능성을 염두에 두고, **다음엔 코드를 더 고치기 전에 먼저
  스크린샷/화면 녹화 등으로 사용자가 실제로 "무엇이 어떻게 다르게 보이는지" 구체적으로
  짚어달라고 요청할 것.** (원칙 2: 검증 없이 "이번엔 진짜 고쳤다"고 다시 단정하지 말 것.)

## 이론 개괄 보고서 (2026-09-24 신규 산출물)

애니메이션 동기화 버그를 디버깅하는 동안, 사용자가 "일단 전체적인 개괄을 봐야 하니" 별도의
정적 HTML 보고서를 요청함 (인터랙티브 애니메이션과는 별개의 산출물).

- **파일 위치**: `guidebook/sfc/theory.html`
- **Artifact 링크**: https://claude.ai/artifact/R2L5XL8hUBV9DSR5h8MQty ("SFC 기동 이론 개괄")
- **목차 10절**: (1)SFC/LCI란 무엇인가 (2)전체 구조 3블록 (3)정류기 점호각→전류크기
  (4)인버터 점호타이밍→주파수 (5)**왜 점호각과 점호타이밍을 맞춰야 하는가**(신규, 아래 참고)
  (6)6-펄스 도통순서 (7)계단형 파형 (8)입출력 주파수 디커플링 (9)전기각 vs 기계각
  (10)기동 시퀀스 전체 그림. 사용자가 애초 제안한 "10. 자주 헷갈리는 오개념 정리" 항목은
  빼달라는 요청으로 제외함.
- **5절 내용은 웹 검색으로 검증**: 사용자가 "토크=계자자속×합성자속의 곱이라 계자자속을
  일정하게 유지하는 게 중요한 것 아니냐"고 질문 → 바로 동의하지 않고 검증한 결과:
  - 토크식 자체(T∝Φ_field×Φ_armature×sinδ)는 맞음, "합성자속"은 고정자 3상 전류가 만드는
    전기자 자속을 가리키는 것으로 해석하면 타당함.
  - 다만 "계자자속을 항상 일정하게 유지"는 부정확 — 실제로는 계자전류가 저속 구간(~10%)에서
    가속 후반(~95% 부근)까지 스케줄에 따라 증가하고, 대신 **전기자측 V/Hz 비율을 일정하게
    유지**하는 게 실제 기법(GE 9FA 등 실제 SFC 기동 사례로 확인).
  - 점호각-점호타이밍을 실시간으로 맞춰야 하는 진짜 이유는 계자자속 자체보다 **폐루프 전류
    제어**: 속도(주파수) 상승 → 역기전력·리액턴스 전압강하 증가 → 같은 전류(토크) 유지하려면
    점호각을 계속 따라 올려야 함.
  - 이 보고서 5절은 위 검증된 내용을 그대로 반영함.
- **디자인**: artifact-design + dataviz 스킬 적용. 다크/라이트 테마 모두 지원, IBM Plex Sans
  KR(본문)+IBM Plex Mono(수식/라벨) 조합, 인터랙티브 시뮬레이션과 계열 통일감 있는 amber/teal
  악센트. 계자전류/V-Hz 스케줄 차트는 `dataviz` 스킬의 팔레트 검증(`validate_palette.js`)
  통과한 aqua(`#1baf7a`/`#199e70`)+amber(`#eda100`/`#c98500`) 조합 사용.

### 페이지 넘김(슬라이드) 형식으로 재구성 (2026-09-24)

사용자 피드백: "내용이 좀 어렵다", "한 섹션을 한 장으로 만들어서 넘겨볼 수 있게", "전기각/위상
내용도 넣어달라", "점호 타이밍에 따라 구형파가 어떻게 형성되는지도 보여달라" → 스크롤형 긴
보고서를 **11장짜리 페이지 넘김(슬라이드) 형식**으로 전면 재구성함 (파일 경로·Artifact URL은
동일, 같은 문서를 갱신).

- **내비게이션**: 이전/다음 버튼, 하단 점(dot) 인디케이터, 키보드 ←→, 터치 스와이프 전부 지원.
  진행률 바 + "n/11" 표시.
- **신규 슬라이드 ① — 전기각과 위상**: 인터랙티브 시뮬레이션 섹션 1(원형 벡터+정현파, R→S→T
  120°씩 위상차)과 **완전히 동일한 수학**(`pt()`, `heightAt()` 함수)으로 그린 정적 스냅샷.
  전기각 vs 기계각 구분도 같은 슬라이드에 노트로 포함.
- **신규 슬라이드 ② — 점호 타이밍이 파형을 만드는 과정**: 1~6 버튼을 눌러 6-펄스 도통 순서를
  한 구간씩 진행하며, 미니 브릿지 다이어그램(현재 도통 중인 2개 소자 강조)과 R/S/T 계단형
  파형이 함께 쌓여가는 걸 보여주는 클릭식 인터랙션. 검증된 STEPS 테이블(T1~T6, topOf/botOf
  매핑)을 그대로 재사용해 시뮬레이션과 수치가 어긋나지 않도록 함.
- **전체 문장 대폭 단순화**: 긴 문단 대신 짧은 불릿 위주로, 한 슬라이드당 정보량을 최소화.
- 계자전류/V-Hz 스케줄 차트는 유지하되 더 작고 단순한 버전으로 축소.
- Artifact v2(같은 URL)에 반영, git 커밋 `7949ac3`.

### 8가지 사용자 피드백 반영 (2026-09-24, v3)

사용자가 슬라이드를 본 뒤 8가지 구체적 개선사항을 요청 → 전부 반영. 그 중 **6번·7번은 실제
기술적 오류를 잡아낸 것**이라 특히 중요:

1. **슬라이드2**: "스스로 회전 토크를 낼 수 없다" 클릭 시 펼쳐지는 설명 추가 — 정지 상태에서
   고정주파수를 걸면 반주기마다 토크 방향이 뒤집혀 평균 토크가 0이 되는 원리.
2. **슬라이드4(전기각과 위상)**: 정적 스냅샷 → **실시간 애니메이션**으로 전환(재생/일시정지
   버튼, 원래 인터랙티브 시뮬레이션과 동일한 `pt()`/`heightAt()` 수식 재사용). 슬라이드가 화면에
   없을 때는 `requestAnimationFrame` 루프가 자동으로 멈추도록 처리.
3. **슬라이드5**: "언제 켤지" 클릭 시 펼쳐지는 설명 추가 — SCR은 점호 신호+순방향 전압에서만
   도통 시작, 늦게 켤수록 사인파의 더 적은 면적을 실어 날라 평균 전압이 줄어드는 원리.
4. **슬라이드5**: **Vdo 정의 추가** — "α=0°일 때 정류기가 낼 수 있는 최대 직류 전압, 입력
   교류 전압 크기로 정해지는 상수, 3상 6-pulse 기준 Vdo≈1.35×선간전압(RMS)".
5. **슬라이드7**: 수동 1~6 버튼 클릭 방식에 더해 **"▶ 자동 시뮬레이션" 재생 버튼** 추가 —
   900ms 간격으로 자동으로 다음 구간을 진행하며 인버터 소자 스위칭에 따른 파형 형성을 실제
   시뮬레이션처럼 보여줌. 슬라이드7을 벗어나면 자동으로 정지.
6. **슬라이드8 토크 벡터 그림 수정 (사용자가 오류 지적, 검증 후 수정)**: 사용자가 "Armature가
   계자자속+전기자자속의 합이냐"고 질문 → **아니다**, 순수하게 고정자 전류만 만드는 자속이며
   "합성(resultant)자속"과는 다른 개념임을 명확히 함. 그림 라벨을 "Φ field (회전자)" /
   "Φ armature (고정자)"로 바꾸고 회전 방향 화살표 추가. "전기자 자속이 전압보다 뒤처져야
   한다"는 사용자의 전제는 **역률(리딩/래깅)에 관한 별개의 축**이며, 오히려 LCI는 자연
   전류형성을 위해 일부러 **진상역률(전류가 전압보다 앞섬)**로 운전한다는 점을 웹 검색으로
   확인해 슬라이드 노트에 추가함 (관련: 검색 결과 "automatic thyristor turn-off is achieved
   with a synchronous motor as the load, if it has a leading phase angle with respect to the
   load voltage").
7. **슬라이드9 문구 오류 수정 (실제 버그)**: 기존 문구가 "속도가 오르면 점호각을 올려야
   한다"였는데, `Vdc = Vdo·cos α` 공식상 **α가 커지면 Vdc는 오히려 줄어들어 모순**이었음.
   "점호각을 **낮춰야**(0°쪽으로) Vdc가 커져서 역기전력을 이긴다"로 정정하고, 인과관계를
   "속도↑ → 역기전력↑ → α↓ → Vdc↑" 한 줄로 정리해 추가.
8. **슬라이드10**: "왜 역기전력이 있어야 LCI 방식으로 전환되는가" 클릭 시 펼쳐지는 설명 추가 —
   커뮤테이션(도통 소자 전환) 시 꺼지는 소자에 역전압을 걸어야 하는데, 이 역전압을 발전기
   자신의 역기전력에서 가져다 쓰는 게 LCI라 저속에서는 역기전력이 없어 실패 → 대신 DC 링크
   전류를 강제로 끊는 방식을 쓰다가, 역기전력이 충분해지면(대략 정격 10% 이상) 자연스러운
   방식으로 전환됨을 설명. 진상역률 운전 이유도 여기서 재확인.

**공통 UI 패턴**: "볼드 텍스트를 누르면 펼쳐지는 설명"은 `.term-toggle` 버튼 +
`.term-detail[hidden]` 패널로 재사용 가능한 컴포넌트를 만들어 1·3·8번에 공통 적용.
- Artifact v3(같은 URL)에 반영, git 커밋 `3989dd5`.

### v4 — "글 대신 그림으로" 대규모 재작업 (2026-09-24)

사용자가 v3의 텍스트 설명들이 "장황하고 이해하기 어렵다"며 **거의 모든 설명을 그림/애니메이션으로
바꿔달라**고 요청 (8개 세부 항목) + 일부 SVG가 화면에서 잘려 보인다는 레이아웃 버그 제보.

- **레이아웃 수정**: `.slide{min-height:420px}` 고정값과 `.visual{flex:1;min-height:0}` +
  `svg{max-height:100%}` 조합이 애매한 퍼센트 높이 계산을 만들어 잘림의 원인으로 추정됨 →
  `.slide`의 고정 min-height 제거(콘텐츠에 맞춰 자연스럽게 높이 결정), `.visual`은 단순히
  `width:100%`인 블록으로, svg는 `width:100%;height:auto;overflow:visible`로 단순화.
  일부 다이어그램은 viewBox 여백도 넉넉히 늘림(예: 토크 벡터 슬라이드 340→380 너비).
- **슬라이드2**: 장문 설명 → 회전자(고정 화살표) 주위를 빠르게 도는 고정자계 화살표 +
  그 아래 좌우로 흔들리는 토크 바늘 애니메이션으로 교체 (패널이 열려있을 때만
  `MutationObserver`로 감지해 `requestAnimationFrame` 재생, 닫히면 자동 정지). 텍스트는 캡션
  한 줄만 유지.
- **슬라이드3**: "이 구분이 이 자료 전체의 핵심 뼈대다" 문장 삭제.
- **슬라이드4 버그 수정**: 위상 애니메이션이 매 360° 랩마다 S·T상이 다시 사라졌다 나타나길
  반복하던 버그 발견 — 원래 인터랙티브 시뮬레이션에 있던 `introDone`(1회성 리빌 래치)을 이
  슬라이드로 옮길 때 빠뜨렸던 것. 추가해서 첫 바퀴에만 R→S(120°)→T(240°) 순서로 자연스럽게
  등장하고, 이후엔 세 상이 계속 남아있도록 수정.
  (사용자가 "갑자기 나오는게 아니라 자연스럽게" 라고 지적한 게 바로 이 버그였음.)
- **슬라이드5**: "언제 켤지" 설명을 사인파 위에 점호 시점을 표시하고 그 지점부터 180°까지의
  면적을 색칠하는 인터랙티브 그림(α=0/30/60/90° 버튼)으로 교체. 오른쪽에 상대 Vdc 막대(cos α에
  비례)도 함께 표시.
- **슬라이드7**: 미니 브릿지에 DC+/DC− 레일과 실제 전류 경로 화살표(원래 인터랙티브 시뮬레이션의
  섹션2 시각언어 재사용)를 추가해서 "어느 소자가 도통하는지"뿐 아니라 "전류가 어떻게 흐르는지"까지
  보이도록 함.
- **슬라이드8**: (a) "왜 토크가 중요한가" — 2절의 "못 도는 문제"와 연결하는 한 줄 노트 추가.
  (b) 각도 호(arc)가 손으로 좌표를 대충 찍어서 벡터와 안 맞고 끊겨 보이던 문제 → `pt()` 함수로
  두 벡터 각도를 정확히 계산해서 호를 그리도록 수정 (더 이상 끊기지 않음). (c) 전류 크기
  버튼(작게/크게)으로 전기자 자속 벡터의 **길이**를 바꿔서, 전류가 클수록 토크가 커지는 관계를
  시각적으로 보여줌 + 상대 토크값 실시간 표시.
- **슬라이드9**: 문장 설명 대신 **역기전력 막대 vs 정류기전압(Vdc) 막대**를 저속/중속/고속
  버튼으로 비교하는 그림으로 교체. Vdc% = min(100, 역기전력%+15), α = acos(Vdc%/100)를 실제로
  계산해서 표시 — 5절의 Vdc=Vdo·cosα 공식과 수치적으로 일관되게 연결됨.
- **슬라이드10**: 장문 설명 → 저속(역기전력≈0, 강제로 꺼야 함) vs 고속(역기전력 충분, 저절로
  꺼짐) **2패널 비교 그림**으로 교체.
- Artifact v4(같은 URL)에 반영, git 커밋 `4a23c04`.

### v5 — 서사 구조 전면 재조정 (2026-09-24)

사용자가 v4에도 "그림 퀄리티가 낮다", "설명에 깊이가 없다", "긁어온 지식을 나열하는 느낌",
"전체 맥락이 안 이어진다"고 재차 지적 → **이번엔 그림을 고치기 전에 먼저 전체 서사 구조를
텍스트로 제안하고 사용자 확인을 받은 뒤** 재작업함 (원칙 1). 사용자가 추가로 "왜 0Hz부터
서서히 올려야 하는지"를 반드시 넣어달라고 요청.

**핵심 변경 — 사실 나열 → 인과관계 사슬.** 11개 본문 슬라이드를 다음 하나의 이야기로 재배열
(각 슬라이드 시작에 "앞에서 ~를 봤다. 그런데 ~" 형태의 bridge 문장을 넣어 연결을 명시함):

1. 문제: 정지 상태 = 동기화 문제로 평균 토크 0
2. **(신규)** 왜 0Hz부터 서서히 올려야 하는가 — (a)시작점은 회전자 속도(0)와 일치해야 함
   (b)회전자 관성 때문에 뒤처지는 각도(δ)가 90°를 넘으면 탈조 — "천천히/급하게" 두 시나리오를
   비교하는 애니메이션(회전자는 가속도 제한 있는 물리모델로 실제 뒤따라가거나 탈조하도록 구현,
   `node`로 수치 검증: 천천히=δ 항상 0, 급하게=1.45초에 90° 초과 실패)
3. 회전자계의 정체 = 전기각/위상 (재생/정지/처음으로 버튼 추가 — 사용자 요청)
4. 이 회전자계를 조절하는 두 손잡이(세기/속도) — 정류기 α, 인버터 타이밍이 왜 필요한지 동기 부여
5-6. 손잡이 ①②
7. 두 손잡이로 실제 회전자계를 만드는 법(파형 형성) — 3절과 "같은 것의 두 관점"임을 명시
8. 회전자계 → 토크: **δ=90°에서 최대, 그 이상 감소하는 sinδ 그래프를 추가**해서 2절의
   "탈조" 현상을 정확한 수식으로 되짚어줌(콜백)
9. 가속의 대가: 토크→가속→역기전력↑→α↓ 사슬을 명시
10. 역기전력의 두 얼굴: 방해꾼이자 동시에 커뮤테이션의 열쇠
11. 완결: 정격 80%, SFC 분리
12. **요약을 불릿 목록 대신 8단계 인과관계 흐름도(화살표로 연결된 카드)로 교체**

**슬라이드 간 상호 참조 번호 오류 수정**: 각 슬라이드 배지 번호(01~11)와 본문 중 "n절에서
본 것처럼" 식의 텍스트 참조가 서로 어긋나 있던 걸 발견해 전부 실제 배지 번호에 맞게 수정함
(예: "7절에서 토크각 δ" → 실제로는 8번 슬라이드였음 → "8절"로 정정).

**그림 품질**: 모든 벡터(화살표) 다이어그램에 공통 `vecArrow()` 헬퍼(선+링+점)를 적용해
슬라이드 간 시각적 일관성을 확보. 화살표 마커도 `arrowMarker()`로 통일.

**전기각/위상 슬라이드**: 재생⇄정지 토글 + "처음으로"(리셋) 버튼 추가 (사용자 요청).

- Artifact v5(같은 URL)에 반영, git 커밋 `84bb8a1`.

### v6 — 7가지 세부 피드백 반영 (2026-09-24)

1. **용어 오류 수정**: 1절 다이어그램에 "고정자계"라고 써있던 라벨(회전하는 자계인데 이름이
   "고정"이라 헷갈림)을 "회전자계"로 통일. 3절(0Hz) 다이어그램의 "자계"도 "회전자계"로 통일.
2. **"미는 힘/당기는 힘" 의미 보강**: 애니메이션에 실시간 상태 텍스트("지금: 회전자를 앞으로
   밀어줌" / "지금: 반대로 당겨서 브레이크가 됨")를 추가하고, 트랙 양끝 라벨을 "제동(역방향)"/
   "가속(순방향)"으로 더 구체적으로 바꿈. 리드 문장도 "앞질렀다 뒤처졌다를 반복"으로 더 명확히.
3. 위 1번 수정으로 3절의 "자계"도 회전자계로 통일 — 2절과의 연결이 끊겨 보이던 문제 해결.
4. **기계각 이야기 삭제**: 3절(전기각과 위상) 슬라이드의 "전기각 vs 기계각" 노트 제거, 제목도
   "위상과 전기각"으로 변경.
5. **손잡이 다이어그램 겹침 버그 수정**: "세기" 박스(x:60~240)와 "속도" 박스(x:220~400)가
   20px 겹쳐 있던 실제 좌표 버그를 발견 → 30px 갭을 둔 레이아웃(30~215, 245~430)으로 재배치.
6. **점호각→전류 연결고리 추가**: Vdc(전압)를 정했다고 끝이 아니라, DC 리액터가 전압차를
   전류로 바꾼다는 것(V=L·di/dt)까지 설명하는 흐름도(전압→리액터→전류) 신규 추가.
7. **(중요) 공간적 권선 배치 설명 추가**: 사용자가 지적한 대로, 회전자계가 생기려면 위상만
   어긋나면 되는 게 아니라 <b>R/S/T 코일이 실제로 고정자에 120° 간격으로 감겨 있어야</b> 한다는
   내용이 완전히 빠져있었음 — 이게 없으면 위상이 어긋나도 제자리에서 세기만 바뀌는 맥동자계에
   그친다는 점까지 포함해서, 고정자 단면도(회전자 + 120° 간격 코일 3개 + 회전 방향 화살표)를
   신규 추가. "회전자계 = 공간적 120° 배치 + 시간적 120° 위상차" 이 두 조건의 결합임을 명시.
- Artifact v6(같은 URL)에 반영, git 커밋 `079d318`.

## 진행 중 이슈 / 다음 세션에서 이어갈 작업

- **"일단 손을 좀 더 봐야할 것 같다"** (사용자 발언, 2026-09-19) — 위 위상
  불일치 버그가 그 코멘트가 가리키던 문제였을 가능성이 있음 (2026-09-24에
  수정 완료). 다만 사용자가 명시적으로 확인해주지는 않았으므로, 이 외에
  추가로 다듬을 부분이 남아있는지는 계속 물어볼 것.
- **4단계(합성벡터 회전)는 아직 계획도 세우지 않음.** 섹션 3의 다듬기가 끝난 뒤,
  원칙대로 계획을 설명하고 승인받은 다음 시작할 것.
- 대상 독자: "완전 초등학생"이 아니라 **신입사원/일반직원(전공지식 없음)** 수준으로
  확정됨 — 비유적 설명(회전목마, 쳇바퀴, 세탁기, 꼬마전동기 등)은 전부 배제하고
  실제 용어(고정자/회전자 등)로 직접 설명하는 방향으로 감.
- 텍스트 설명 대신 그림/애니메이션으로 최대한 직관적으로 전달하는 것이 목표
  (섹션 1~3에 곁텍스트 없이 그림만으로 이해되도록 이미 다듬어짐 — 색상 의미,
  0/1 구분, RST 120° 배치, +/−상 표시 등 사용자 피드백 반영 완료).
